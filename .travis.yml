language: r
r:
  - oldrel
  - release
  - devel
cache: packages
matrix:
  include:
    - os: linux
      dist: precise
      sudo: false
    - os: linux
      dist: trusty
      sudo: required
    - os: osx
      osx_image: xcode8
    - os: osx
      osx_image: beta-xcode6.2
notifications:
  email:
    - jdunham@mit.edu
r_github_packages:
  - jimhester/covr
after_success:
  - Rscript -e 'library(covr); codecov(line_exclusions = list("src/include/models.hpp"))'
